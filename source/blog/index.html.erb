<% blog.articles.each do |article| %>
  <dl>
      <header>
        <dt>
          <a href="<%= article.url %>">
          <%= article.title %>
        </a>
        </dt>
        <time>
          <%= article.date.strftime("%B %e, %Y") %>
        </time>
      </header>
      <p>
        <%= article.metadata[:page]["description"] %>
      </p>
  </dl>
<% end %>
